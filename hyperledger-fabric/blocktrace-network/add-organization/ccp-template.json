{
    "name": "blocktrace-network-${ORG_NAME}",
    "version": "1.0.0",
    "client": {
        "organization": "${ORG_NAME_UPPER}",
        "connection": {
            "timeout": {
                "peer": {
                    "endorser": "300"
                }
            }
        }
    },
    "organizations": {
        "${ORG_NAME_UPPER}": {
            "mspid": "${ORG_NAME_UPPER}",
            "peers": [
                "peer0.${ORG_NAME}.blocktrace.com"
            ],
            "certificateAuthorities": [
                "ca.${ORG_NAME}.blocktrace.com"
            ]
        }
    },
    "peers": {
        "peer0.${ORG_NAME}.blocktrace.com": {
            "url": "grpcs://localhost:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer0.${ORG_NAME}.blocktrace.com",
                "hostnameOverride": "peer0.${ORG_NAME}.blocktrace.com"
            }
        }
    },
    "certificateAuthorities": {
        "ca.${ORG_NAME}.blocktrace.com": {
            "url": "https://localhost:${CAPORT}",
            "caName": "ca-${ORG_NAME}",
            "tlsCACerts": {
                "pem": ["${CAPEM}"]
            },
            "httpOptions": {
                "verify": false
            }
        }
    }
}
